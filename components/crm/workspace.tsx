import{Dashboard}from"@/components/crm/dashboard";import{getDashboardData}from"@/lib/crm/dashboard";import type{AppRole}from"@/lib/types";
export async function Workspace({section,slug,role,userId}:{section:string;slug?:string[];role:AppRole;userId:string}){const page=(slug?.join(" / ")||"dashboard").replaceAll("-"," ");if(page==="dashboard"){const data=await getDashboardData(role,userId);return <Dashboard data={data}/>}return <section className="space-y-6"><div><div className="text-sm font-medium text-[#1c6b43] uppercase tracking-wide">{section}</div><h1 className="mt-1 text-3xl font-semibold capitalize">{page}</h1><p className="mt-2 text-slate-500">Production module shell connected through shared services, permissions and audit-ready data architecture.</p></div><div className="rounded-2xl bg-white border border-[#dfe7e1] p-6"><div className="font-semibold">Module workspace</div><p className="mt-2 text-sm text-slate-500">Search, filters, tables/forms, activity history and database actions belong to module services rather than UI components.</p></div></section>}