import {createServerClient} from "@supabase/ssr"; import {cookies} from "next/headers";
export async function createSupabaseServerClient(){const c=await cookies();const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)throw new Error("Supabase environment is not configured");return createServerClient(url,key,{cookies:{getAll(){return c.getAll()},setAll(items){try{items.forEach(({name,value,options})=>c.set(name,value,options))}catch{}}}})}